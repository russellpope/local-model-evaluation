# vCenter connection settings for vsphere-inventory.
# Use with: vsphere-inventory --config config.yaml vms
#
# Precedence (highest first):
#   command-line flag > VSPHERE_* environment variable > this file > default

url: https://vc.lab/sdk
username: administrator@vsphere.local
password: change-me
insecure: false
timeout: 60s
