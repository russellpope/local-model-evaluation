# Example connection config for vsphere-inventory.
# Pass with:  vsphere-inventory vms --config examples/config.yaml
# or place at any path and reference it from a flag/env layer.
#
# Precedence (highest first): --flag > VSPHERE_* env > this file > built-in default.
url: https://vc.lab/sdk
username: administrator@vsphere.local
password: Secret123!
insecure: true
timeout: 60s
